import { ImageStyle } from 'react-native';
import { ImageURISource } from 'react-native';
import { ReactElement } from 'react';
import { StyleProp } from 'react-native';

declare function Image_2({ testID, source, style, color }: ImageProps): ReactElement | null;
export { Image_2 as Image }

declare interface ImageProps {
    source?: NativeImage;
    style?: StyleProp<ImageStyle>;
    color?: string;
    testID?: string;
}

/**
 * Prop value representing images for the native platform.
 *
 * @see ImageURISource
 */
declare type NativeImage = Readonly<ImageURISource | string | number>;

export { }
